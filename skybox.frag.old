#version 150

/*
in vec3 texcoords;
uniform sampler cube_texture;
out vec4 frag_colour;

void main (void) {
  frag_colour = texture (cube_texture, texcoords);
}
*/

out vec4 frag_colour;
void main(void){
	out frag_colour = vec4(1.0,1.0,1.0,1.0);
}